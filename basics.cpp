// Every C++ function has a main function, it is the entry point of every program.
#include <iostream>

int main(void) {
    std::cout << "Hello, World!\n";
    
    // "main" returns a number to OS
    // The number is 0 if the program finishes as expected, is any other number if error occurs.
    return 0;
}
